import { csrfFetch } from "./csrf";
/*-------------Types-------------*/

/*-------------ACTIONS-------------*/

/*-------------THUNK CREATORS-------------*/

/*-------------REDUCERS-------------*/
const initialState = {};
const notesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default notesReducer;
